FROM base AS worker-base
WORKDIR /app/worker

COPY worker/ ./

ARG DATABASE_URL
ENV DATABASE_URL=$DATABASE_URL

RUN pnpm install
RUN pnpm run build

CMD cd ../packages/database && pnpm prisma migrate deploy && cd ../../worker && pnpm start
