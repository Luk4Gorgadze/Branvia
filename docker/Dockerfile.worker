FROM base AS worker-base
WORKDIR /app/worker

COPY worker/ ./

# DATABASE_URL is already available from base image, no need to redeclare
RUN echo "DATABASE_URL from base: $DATABASE_URL"

RUN pnpm install
RUN echo "📦 Building Worker application..."
RUN pnpm run build

CMD ["pnpm", "start"]
